apiVersion: v1
kind: Service
metadata:
  name: auth-grpc
  namespace: auth
  labels:
    app: auth-grpc
spec:
  type: ClusterIP
  selector:
    app: auth-grpc
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
