apiVersion: v1
kind: Service
metadata:
  name: health
  namespace: default
spec:
  type: ExternalName
  externalName: auth-api.auth.svc.cluster.local
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http

