apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../base

patches:
- path: patch-argocd-ingress.yaml
- path: patch-grafana-ingress.yaml

# replacements는 Secret이 있어야 동작하므로 제거
# ACM ARN은 base에서 PLACEHOLDER로 유지하거나
# AWS Load Balancer Controller가 ALB group level에서 관리
