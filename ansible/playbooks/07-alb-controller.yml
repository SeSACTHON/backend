# AWS Load Balancer Controller 설치
# 공식 문서: https://kubernetes-sigs.github.io/aws-load-balancer-controller/
# Note: import_tasks로 호출되므로 tasks만 정의 (hosts 제거)
---
- name: "ArgoCD가 ALB Controller를 관리합니다 (Wave 20)"
  debug:
    msg:
      - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      - "ℹ️  ArgoCD Application (`argocd/apps/20-alb-controller.yaml`)이"
      - "   eks/aws-load-balancer-controller Helm Chart를 자동 배포합니다."
      - "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      - ""
      - "📍 배포 시점: ArgoCD root-app 배포 후 Wave 20"
      - "📍 설치 위치: kube-system namespace"
      - ""
      - "💡 수동 설치가 필요한 경우:"
      - "   ansible-playbook site.yml -e helm_managed_alb=false"
      - ""
