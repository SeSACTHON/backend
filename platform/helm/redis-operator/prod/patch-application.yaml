apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-operator
spec:
  project: prod
  source:
    helm:
      valuesObject:
        redisOperator:
          watchNamespace: ""
        service:
          namespace: data-system
        nodeSelector:
          role: control-plane
        tolerations:
          - key: node-role.kubernetes.io/control-plane
            operator: Exists
            effect: NoSchedule
          - key: role
            operator: Equal
            value: control-plane
            effect: NoSchedule

