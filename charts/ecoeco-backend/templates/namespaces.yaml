# Namespace 리소스 정의
# 14 Node Microservices Architecture
# 
# 현재 구조: api 단일 네임스페이스 (모든 API 통합)
# 참고: docs/architecture/09-NAMESPACE_STRATEGY_ANALYSIS.md

---
apiVersion: v1
kind: Namespace
metadata:
  name: api
  labels:
    name: api
    app.kubernetes.io/part-of: ecoeco-backend
    app.kubernetes.io/component: api-layer
